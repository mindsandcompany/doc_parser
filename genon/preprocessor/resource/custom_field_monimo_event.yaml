# ─────────────────────────────────────────────────────────────────────────
#  v2 스키마로 자동 변환된 설정입니다.
#
#  자동 변환은 **값만** 옮깁니다. 아래는 원본 v1 의 주석 전문이며, 필요한 설명을 각
#  필드 스펙 옆으로 손수 옮긴 뒤 이 블록을 지우세요. 주석이 사라지면 왜 그런 설정인지
#  아는 사람이 없어집니다.
# ─────────────────────────────────────────────────────────────────────────
# 이벤트 JSON 레코드 → TB_EVENT 목표필드 직접 매핑 (모니모)
#
# 사용 (parser_processor_config.yaml):
#   - custom_fields:
#       enable: true
#       doc_type: monimo_event
#       extractor: json_mapping
#       config_file: custom_field_monimo_event.yaml
#
# 원천: monimo_rag_adev_yyyymmdd_full_001.json (공통, 전 관계사 혼재)
#
# 이 파일 하나로 자족한다 — 별도 프롬프트 md 나 LLM config yaml 이 필요 없다.
#   요약 생성에 쓰는 LLM 연결/프롬프트까지 아래 llm_fields 안에 인라인되어 있다.
#   (프롬프트가 길거나 여러 doc_type 이 공유한다면 llm_fields 항목에 config_file 을 대신 써도 된다
#    — custom_field_card.yaml 과 같은 방식. 두 방식은 같은 코드 경로로 처리된다.)
#
# 목표필드명은 TB_EVENT 컬럼명(대문자)을 그대로 쓴다.
#
# 입력 예시(협의용 한글 키 표기):
#   {"eventList": [{"ID": "CM26061182", "회사명": "모니모", "제목": "…",
#                   "이벤트 시작일": "26.07.01", "이벤트 종료일": "26.07.31",
#                   "wcmsHtml": {"htmlText": "<div>이벤트 상세 …</div>"}}]}
#
# 실 payload(원천 화면에서 확인): 키가 영문 camelCase 이고 회사명 필드가 없다.
#   {"resultCode": "0000", "eventList": [
#     {"cmpId": "M261106191", "cmsId": "3750598", "evtTodayMainCopy": "하이마트 구독을 …",
#      "evtHeaderTopTitle": "하이마트 구독을 가볍게 ", "evtHeaderTitle": "… <br/>…",
#      "evtTodaySubCopy": "26.07.01~26.07.31", "evtPtrmStrtDt": "20260710",
#      "evtPtrmEndDt": "20261111", "evtDvC": "F", "buttonLink": "https://…",
#      "wcmsHtml": {"htmlText": "<div class=\"box940 mt0\">…"}}]}
#   → 두 표기를 모두 받도록 key_map 별칭에 실 payload 키를 함께 등록해 두었다.
#      샘플: sample_files/monimo/monimo_event_real_sample.json (실 스키마)
#            sample_files/json/monimo_event_sample.json        (한글 키 표기)
# ── 레코드 배열이 담긴 key. 키 이름만 쓴다(임의 깊이 재귀 매칭). 생략 시 payload 전체가 1건 ──
# ── 목표필드: [허용 소스 key 별칭 목록] ─────────────────────────────────────
# 목표필드명 자체도 자동 별칭으로 포함된다. 비교 시 Unicode/BOM/공백/대소문자/_,-,. 차이를 정규화한다.
# 값은 레코드 안 임의 깊이에서 찾되 얕은 쪽이 우선한다(wcmsHtml.htmlText → htmlText 한 단어로 매칭).
# 같은 레벨 안에서는 별칭 선언 순서가 우선순위다 — 실 payload 키는 한글 키 뒤에 덧붙인다.
                                                  # 실 payload 는 cmpId(캠페인) + cmsId(콘텐츠) 2개다.
                                                  # 어느 쪽이 적재 유일키인지 확정되면 순서를 조정할 것.
                                                  # 대표 제목 (NOT NULL). 실 payload 우선순위:
                                                  # evtTodayMainCopy(태그 없는 전체 문구)
                                                  # → evtHeaderTopTitle(짧은 상단 제목)
                                                  # → evtHeaderTitle(<br/> 가 섞여 있어 차선)
                                                  # 실 payload 는 "20260710" 8자리 — date_int_flex 가 처리한다.
                                                  # evtTodaySubCopy("26.07.01~26.07.31") 는 범위 문자열이라 쓰지 않는다.
                                                  # 원문 그대로 청크 metadata 에 실린다. 상세가
                                                  # 수십~수백 KB 면 청크 payload 도 그만큼 커지므로,
                                                  # 적재 측에서 별도 컬럼으로 받는지 확인할 것.
# ── 값 별칭 → 표준 코드 ─────────────────────────────────────────────────────
# ── 필수 목표필드. 값이 없는 레코드는 그 건만 skip 하고 건수를 경고로 남긴다 ──
# GROUP_C 는 여기서 뺐다 — 실 payload 에 회사 구분 필드가 없어 필수로 두면 전 레코드가 skip 된다.
# 아래 defaults 로 채우고, 적재 측 NOT NULL 은 그 기본값으로 만족시킨다.
# ── 대응 소스가 없을 때 채울 기본값 ─────────────────────────────────────────
# GROUP_C: 위 value_map 을 타고 IFP 로 접힌다(defaults → value_map 순서).
# 원천(monimo_rag_adev_*_full_*.json)은 전 관계사가 혼재한다고 안내받았는데, 실 payload 에는
#    관계사를 가릴 필드가 보이지 않는다(evtDvC: "F" 의 의미 미확인). 전건을 IFP 로 채우는 것은
#    잠정 조치다 — 관계사 판별 필드를 확인해 GROUP_C 별칭으로 추가해야 한다.
# ── 소스와 무관하게 고정으로 채울 값 ────────────────────────────────────────
# 공통 표준 레이아웃에는 노출 게이트 필드가 없다(받은 원본의 monimo_hidden 대응 여부 확인 필요).
# TB_EVENT 기본값과 같은 'N' 으로 두고, 적재 측에서 게시 승인 시 올리는 것을 전제로 한다.
# constants:
#   SEARCHABLE_YN: "N"
# ── 값 변환 (field_transforms.VALUE_TRANSFORMS 에 등록된 이름) ───────────────
# date_int_flex: "26.07.01" / "2026-07-01" 모두 20260701 정수로. 파싱 실패 시 0.
# ── HTML 필드 → 평문 파생 필드 (LLM 입력용) ─────────────────────────────────
# 파싱 경로와 같은 정리 규칙(html_flatten)을 쓴다 — aria-hidden/접힌 약관 텍스트는 보존.
# ── JSON 에 없는 필드를 LLM 으로 생성 ───────────────────────────────────────
# 레코드마다 1회 호출한다. input_fields/concurrency/on_error 를 뺀 나머지 키는 그대로
# CustomFieldsEnricher 생성자로 넘어간다(= custom_field_card.yaml 의 키들과 동일한 스키마).
# llm_fields:
#   - # 무엇을 만들 것인가
#    # 이 필드는 예전에 CONTENT_HASH 로 잘못 적혀 있었다. TB_* 의 CONTENT_HASH 는
#     #    RAW(32) 원문 검증 해시이고, 요약본문(임베딩 입력)은 SUMMARY_TEXT 다.
#     output_fields: [SUMMARY_TEXT]        # 요약본문 (임베딩 입력)
#     input_fields: [TITLE, DETAIL_TEXT]   # 프롬프트 {{raw_text}} 로 들어갈 필드(순서대로 결합)
#     concurrency: 4                       # 레코드 동시 호출 상한
#     on_error: null                       # null(필드를 null 로 두고 진행) | skip_record
#     # ── LLM 연결 ────────────────────────────────────────────────────────────
#     # <ENRICHMENT_SERVING_ID>: Genos에 등록한 모델서빙 ID로 변경 필요. api_key 도 배포 시 설정.
#     url: "http://llmops-gateway-api-service:8080/rep/serving/<ENRICHMENT_SERVING_ID>/v1/chat/completions"
#     api_key: ""
#     model: model
#     # ── LLM 생성 파라미터 ───────────────────────────────────────────────────
#     # 요약본문이 임베딩 입력이므로 과도하게 길지 않게 잡는다.
#     max_tokens: 1000
#     temperature: 0.0
#     timeout: 120
#     # ── 응답 파싱 ───────────────────────────────────────────────────────────
#     parser:
#       type: json
#     # ── 프롬프트 ────────────────────────────────────────────────────────────
#     # output_fields 와 프롬프트가 내놓는 JSON 키(SUMMARY_TEXT)는 반드시 같아야 한다.
#     # 어긋나면 에러 없이 값이 비어서 나온다.
#     system_prompt: |
#       너는 이벤트 안내문 요약 전문가다. 주어진 이벤트 상세 내용을 검색용 임베딩에 넣을 요약본문으로 압축한다.
#       규칙:
#       - 반드시 하나의 JSON 객체만 반환한다. 설명 문구·마크다운 코드펜스 없이 순수 JSON 만 출력한다.
#       - 키는 `SUMMARY_TEXT` 하나다. 값은 요약본문 문자열이다.
#       - 원문에 없는 정보를 지어내지 않는다(환각 금지).
#       - 요약본문에는 이용자가 검색할 만한 표현을 남긴다: 이벤트 제목, 대상 브랜드·제휴사, 혜택 종류와 금액/비율,
#         응모·참여 방법, 기간 조건. 배너 문구·버튼 라벨·법적 고지 상용구는 제외한다.
#       - 문장형 한국어로 3~6문장, 800자 이내로 쓴다. 불릿·머리기호를 쓰지 않는다.
#       - 상세 내용이 비어 있거나 요약할 내용이 없으면 값을 null 로 둔다.
#       출력 예시 형식(값은 예시):
#       {"SUMMARY_TEXT": "무신사 블랙 프라이데이 기간 동안 모니모 카드로 결제하면 …"}
#     user_prompt: |
#       아래 이벤트 정보를 검색용 요약본문으로 압축하라.
#       - SUMMARY_TEXT (요약본문) — 필수
#       반드시 `SUMMARY_TEXT` 키 하나를 가진 JSON 객체로만 반환하라.
#       <event>
#       {{raw_text}}
#       </event>
# ── [선택] 청크 본문에 함께 실을 항목명 ─────────────────────────────────────
# 본문 필드를 `항목명: 값` 으로 낸다. 여기 이름이 없는 필드는 값만 나간다.
# 사람이 검색어로 쓰는 말을 적는다 — 목표필드명(DB 컬럼명)이나 원천 key 는 검색어에 안 나온다.
# 값에 줄바꿈이 있는 블록(상세 본문 등)에는 붙지 않는다. 그 블록은 이미 자기 제목을 갖는다.
# ── 청크 text 본문 구성 순서 (개행 결합) ─────────────────────────────────────
# 상세 원문 HTML 은 DETAIL_HTML metadata 로만 싣고, 임베딩 입력은 요약본문을 쓴다.
# 제목을 앞에 두는 이유: LLM 미설정/실패로 요약이 비면 본문이 통째로 비어 그 레코드가
# element 에서 제외된다(경고 로그). 제목만이라도 남겨 검색 가능한 상태를 유지한다.
# ── 레코드 본문이 chunk_size 를 넘으면 여러 청크로 나눈다(metadata 는 조각마다 동일) ──
# ── records 키를 찾지 못했을 때: error(기본, 즉시 실패) | skip(경고 후 0건) ──

schema: v2
source:
  kind: records
  records_at: eventList
  on_missing: error
fields:
  BIZ_ID:
    alias: [cmpCntsId, ID, 이벤트ID, event_id]
    default: null
  GROUP_C:
    alias: [mnmFncoCd, 회사명, corp_code, 고객사]
    default: IFP
    values:
      IFP: [0, 모니모]
      SLF: [2, 삼성생명, 생명]
      SSF: [3, 삼성화재, 화재]
      HPP: [1, 삼성카드, 카드]
      SSS: [4, 삼성증권, 증권]
  TITLE:
    alias: [text, 제목, title, evtTodayMainCopy]
    transform: text_norm
  EVENT_FROM:
    alias: [이벤트 시작일, 시작일, startDate, start_date, evtPtrmStrtDt]
    transform: date_int_flex
  EVENT_TO:
    alias: [이벤트 종료일, 종료일, endDate, end_date, evtPtrmEndDt]
    transform: date_int_flex
  DETAIL_HTML:
    alias: [cmp_desc, htmlText, 상세내용]
  # DETAIL_HTML 과 **같은 원천**을 한 번 더 읽어 평문 사본을 만든다. 원본 HTML 은
  # metadata 로만 싣고 임베딩 입력은 이 평문을 쓴다.
  # `html_text` 는 HTML 로 강제 평문화한다(표·목록 구조 유지).
  DETAIL_TEXT:
    alias: [cmp_desc, htmlText, 상세내용]
    transform: html_text
  MOVE_TARGET_URL:
    alias: [etcMoveTargetPage]
  EVENT_KIND_C:
    alias: [evnKndCd]
  CAMPAIGN_C:
    alias: [cmpCode]
require:
  fields: [TITLE]
body:
  fields: [TITLE, DETAIL_TEXT]
  labels:
    TITLE: 제목
    DETAIL_TEXT: 상세내용
  split: true
  repeat: [TITLE]
