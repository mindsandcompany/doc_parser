name: CI (PR → develop)
on:
  pull_request:
    branches: [ develop ]
  workflow_dispatch: { }

jobs:
  pr-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ '3.12', '3.13' ]
    env:
      # facade.* 는 genon/preprocessor 에서, genon.preprocessor.facade.* 절대 import 는 repo root 에서
      # 해석되어야 하므로 두 경로 모두 포함(tests/conftest.py 와 동일). Import smoke 는 conftest 미적용이라
      # PYTHONPATH env 에만 의존한다.
      PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/genon/preprocessor
      # DOCLING_ARTIFACTS_PATH를 설정하지 않아 자동 다운로드 사용
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - uses: astral-sh/setup-uv@v6

      - run: uv python pin ${{ matrix.python-version }}

      - name: Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt/archives
          key: apt-${{ runner.os }}-libreoffice-${{ hashFiles('.github/workflows/develop.yml') }}
          restore-keys: apt-${{ runner.os }}-libreoffice-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libreoffice poppler-utils tesseract-ocr tesseract-ocr-kor

      # 여기부터는 도커파일처럼 preprocessor 안에서 실행
      - name: Sync deps (preprocessor)
        working-directory: genon/preprocessor
        run: |
          uv sync --dev

#      - name: Install additional Python packages
#        working-directory: genon/preprocessor
#        run: |
#          uv pip install --upgrade "unstructured>=0.8.0,<0.10.0" \
#            pdf2image>=1.16.0 \
#            pdfminer.six>=20220524 \
#            pytesseract>=0.3.10 \
#            weasyprint>=60.0 \
#            opencv-python>=4.8.0

      - name: Download HWP SDK from HuggingFace
        working-directory: genon/preprocessor
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          HWP_SDK_DIR="${{ github.workspace }}/hwp_sdk"
          mkdir -p "$HWP_SDK_DIR"
          uv run huggingface-cli download genon-search/hwp_sdk \
            --repo-type dataset \
            --local-dir "$HWP_SDK_DIR" \
            --local-dir-use-symlinks False \
            --token "$HF_TOKEN"
          chmod +x "$HWP_SDK_DIR/convtext"
          SITE_PKG=$(uv run python -c "import site; print(site.getsitepackages()[0])")
          ln -sf "$HWP_SDK_DIR" "${SITE_PKG}/hwp_sdk"

      - name: Download NLTK data
        working-directory: genon/preprocessor
        run: |
          uv run python -c "import nltk; nltk.download('averaged_perceptron_tagger_eng'); nltk.download('punkt_tab')"

      - name: Download custom layout model from HuggingFace
        working-directory: genon/preprocessor
        run: |
          uv run python -c "from docling.models.layout_model import LayoutModel; from docling.datamodel.layout_model_specs import MNCAI_CUSTOM_LAYOUT; print('Downloading custom layout model...'); model_path = LayoutModel.download_models(layout_model_config=MNCAI_CUSTOM_LAYOUT, progress=True); print(f'Model downloaded to: { model_path }')"
      - name: Compile sources
        working-directory: genon/preprocessor
        run: uv run python -m compileall -q . -x 'olefile'

      - name: Import smoke
        working-directory: genon/preprocessor
        run: uv run python -c "import importlib; importlib.import_module('facade.attachment_processor')"

      - name: Pytest (unit+smoke+regression)
        working-directory: genon/preprocessor
        run: uv run pytest tests -m "unit or smoke or regression" -v -ra --durations=10

      - name: Build package
        working-directory: genon/preprocessor
        run: uv build
