# facade 정리 — 작업 계획 (#363)

parser / chunking 프로세서를 **고객이 직접 읽고 수정할 수 있는 크기**로 줄인다.

## 이 작업의 목적

현재 프로세서가 제공하는 기능을 고객 사이트에 제공하되, **기존 방식으로 처리하기 어려운
새 문서를 만났을 때 고객이 간소화된 parser / chunking 프로세서를 직접 고쳐서 해결**할 수
있게 만드는 것이 목적이다.

따라서 이번 작업의 산출은 세 가지다.

1. 프로세서가 읽을 수 있는 크기가 된다 (01~03)
2. 고칠 자리가 어디인지, 지금 있는 기능으로 어디까지 되는지가 드러난다 (04~05)
3. 실제 문서로 "정말 되는지" 를 확인한다 (06)

## 최우선 원칙 두 가지

### 1. 동작 동일성

**리팩터링 뒤 parser / chunker 의 산출이 기존과 같아야 한다. 다른 모든 목표에 우선한다.**

크기를 줄이는 것도, 표면을 정리하는 것도 이 조건을 깨면서 하지 않는다.
그래서 00번(골든 기준선)이 다른 모든 작업보다 먼저다.

### 2. 신규 기능을 추가하지 않는다

이번 작업은 **정리와 재배치만** 한다. 새 설정 키, 새 확장 메커니즘, 새 플러그인 지점을
만들지 않는다. 고객의 새 요건은 **지금 있는 기능**으로 푸는 것이 전제이고,
그것으로 안 될 때 고객이 프로세서 코드를 고치는 것이 이번 리팩터링이 여는 길이다.

기능 추가 아이디어가 나오면 별도 이슈로 분리한다.

## 문서

| 문서 | 내용 | 성격 |
|---|---|---|
| [WORKFLOW](WORKFLOW.md) | 임시 작업본과 A/B 대조 — 각 단계 공통 절차 | **선행 참조** |
| [00](00-golden-baseline.md) | 골든 출력 회귀 하네스 | **선행 필수** |
| [01](01-chunking-dead-code.md) | `chunking_processor.py` 미사용 파싱 런타임 제거 | 정리 |
| [02](02-docling-runtime.md) | docling 런타임을 공용 모듈로 추출 | 라이브러리화 |
| [03](03-parser-decompose.md) | `parser_processor.py` 분해 | 라이브러리화 |
| [04](04-user-surface.md) | 고객이 고칠 자리 확정 | 표면 정리 |
| [05](05-customer-extension.md) | 지금 기능으로 어디까지 되는가 · 어디서부터 코드인가 | 조사·문서화 |
| [06](06-new-doctype-drill.md) | 신규 문서 실전 드릴 — 정말 processor 수정만으로 되는가 | **최종 판정** |
| [07](07-customer-change-lifecycle.md) | 고객 수정분의 수명주기 | **선행 결정** |
| [08](08-customer-facade.md) | 고객 개발자용 100줄 facade + 확장 훅 | **신규 단계** |
| [09](09-flow-open.md) | 처리 흐름을 파사드가 갖게 한다 + 문서 종류별 설정 | **후속 단계 · 미착수** |
| [09-B](09-special-cases.md) | 특이 케이스 20건 — facade 코드로 어디까지 되는가 | 조사·검증 |

01~03 은 순서 의존이 있다(01 이 02 의 대상을 줄이고, 02 가 03 의 전제다).
04·05 는 그 뒤다 — 줄어든 파일 위에서 표면을 정해야 두 번 일하지 않는다.
06 이 마지막이며 **이 작업의 성공을 판정하는 단계**다. 줄 수가 아니라 06 의 결과가 판정 기준이다.

**07 은 01~06 과 독립이며 우선순위가 가장 높다.** 고객이 고친 코드가 다음 릴리스에서
어떻게 되는지가 정해지지 않으면 나머지 작업의 목적이 성립하지 않는다.

## 진행 상태 (2026-09-09)

| 단계 | 상태 | 결과 |
|---|---|---|
| 00 골든 기준선 | **완료** | 56케이스 112산출물. `--record`/`--check`/`--noise` + `--output-format` 축 |
| 01 청커 죽은 코드 | **완료** | 1,536 → 1,217줄 |
| 02 docling 런타임 | **완료** | facade 3종 −1,110, 신규 공용 모듈 +523 |
| 03 parser 분해 | **완료** | 2,497 → 1,605줄 (3a/3b/3c) |
| 04 고칠 자리 확정 | **완료** | `ROUTES` 라우팅 표 + 고칠 자리 3개 + 문서 4종 |
| 05 기능 경계 문서화 | **완료** | 조사 9항목을 `gitbook_doc/parser_processor.md` 로 |
| 06 실전 드릴 | **완료** | **합격 기준 6개 전부 통과** ([결과](06-drill-results.md)) |
| 07 고객 수정 수명주기 | **A 결정됨** | 08 이 A3(고객 소유 파일 분리)로 확정. B·C·D 는 08-5 |
| 08 고객용 facade | **완료(08-5 일부 보류)** | facade 2종 1,644+1,223 → **92+100줄**. 기존 doc_type 13케이스를 훅 26줄로([08-B](08-B-ablation-results.md)), 신규 문서 7변형을 24줄로([08-4](08-4-format-drill-results.md)) 재현. A3 확정만 남았다 |

| 09 흐름 공개 | **미착수** | 역할극 검증 2회로 계획 수립. 결정 4건 확정 후 착수([09](09-flow-open.md)) |

각 단계는 골든 56케이스 차이 0 과 표적 유닛 실패 집합 동일(착수 전 40건 베이스라인)로 확인했다.

## 06 판정 — 이 작업의 성공 여부

| 기준 | 목표 | 실측 |
|---|---|---|
| 고쳐야 하는 파일 | facade 1개 | `parser_processor.py` 1개 |
| 고치는 위치 | 04 가 정한 "고칠 자리" 안 | 전부 `_load_json_payload`(앵커 3번) |
| 변경 줄 수 | 30줄 이하 | 27 삽입 / 3 삭제 |
| 기존 doc_type 산출 | 골든 차이 0 | 56케이스 차이 0 |
| 청커 산출 | 새 doc_type metadata 가 청크에 | 15/15 `custom_fields_row` |
| 원인 파악 | 파일 전체를 읽지 않고 도달 | 파일 머리 주석에서 한 번에 |

**공용 모듈은 한 줄도 건드리지 않았다.** 신규 JSON 15종 중 **8종이 설정만으로** 처리됐고
4종이 코드를, 3종이 경계 확인으로 끝났다.

## 남은 것

- **07-A 는 [08](08-customer-facade.md) 이 A3(고객 소유 `preprocessor.py` 를 배포 범위에서
  분리)로 확정했다.** facade 가 100줄이 되어 벤더 로직과 고객 수정의 분리가 성립하기 때문이다.
  B(번들 경고)·C(배포 단위 2개)·D(고객 골든)는 08-5 에서 문서로 처리한다.
- 별도 이슈 후보 5건: 빈 레코드 배열에서 청커 예외(06 B15), 청커의
  `page_batch_size` 전역 오염(01), xlsx 기본값·`thinking` 인자 드리프트(02),
  sections/document 표 평문화 렌더러 주입(05 경계 ⑤), 배열 metadata 의 repr 표기(06 B5).

착수 중 내린 결정 4가지(계획에 없던 것):

1. **골든에서 빼는 케이스 3종** — `card01.flat.html`(개인 디렉터리), `hwp_sample_table.hwp`
   (외부 실행파일 `convtext` 필요), `pptx_sample.pptx`(PPT→PDF 변환본이 실행마다 달라져
   layout VLM 캐시가 매번 miss, 실측 17건). 뺐다는 사실은 실행할 때마다 출력한다.
   docling PDF 경로는 `pdf_sample.pdf` 가 덮으며 이쪽은 안정적이다.
2. **노이즈 측정은 record/check 와 같은 캐시 스코프로** — 회차마다 스코프를 나누면 2회차가
   LLM 을 다시 불러 그 답의 흔들림까지 노이즈로 잡힌다(실측: text/n_char/SUMMARY_TEXT 등
   58개 산출물이 어긋났다). 스코프를 맞추면 남는 노이즈는 `reg_date` 하나다.
3. **3a 에서 `GenericDocumentLoader` 는 facade 에 남긴다** — `get_loader` 가 곧
   "어떤 확장자를 어느 로더로 보내는가" 라서 04·05 가 고객에게 읽히려는 바로 그 표다.
   옮긴 것은 `HwpDocumentLoader`/`DocxDocumentLoader`(순수 docling 컨버터 조립)뿐이고,
   그 덕에 계획이 걸림돌로 꼽은 두 가지(공용 모듈의 docling import, 공용 모듈이 어느
   `GenosServiceException` 을 쓰나)가 함께 사라졌다.
4. **A/B 대조는 `*_wip.py` 대신 "HEAD 사본 vs 현재 트리" in-process 비교로 했다** —
   `git show HEAD:<파일>` 을 별도 모듈로 로드해 같은 프로세스에서 양쪽 인스턴스를 만들고
   `__dict__` 를 통째로 대조했다. WORKFLOW 의 임시본 방식보다 이쪽이 `__init__` 리팩터링에
   더 맞는다 — 임시본은 "wip == 현재 트리" 만 증명하는데, 이 방식은 "현재 == 착수 전" 을
   직접 증명한다. 임시본을 만들지 않았으므로 종료조건 4·5(임시 파일 삭제·`git status` 청결)는
   자동으로 만족한다.
5. **드리프트 2개는 고치지 않고 플래그로 보존** — `formats.xlsx.processing_mode` 기본값
   (parser=tabular / 나머지=docling)과 `MetadataEnricher` 의 `thinking` 인자
   (intelligent 만 안 넘긴다). 통일은 동작 변경이라 별도 이슈다.

## 현재 규모 (실측 2026-09-05)

| 파일 | 줄수 | 목표(예상) |
|---|---:|---:|
| `facade/parser_processor.py` | 2,497 | **~1,400** |
| `facade/chunking_processor.py` | 1,536 | ~1,180 |
| `facade/intelligent_processor.py` | 1,458 | ~1,100 |
| `facade/convert_processor.py` | 1,600 | ~1,250 |
| `facade/attachment_processor.py` | 1,620 | 범위 밖 |
| 합계 | 8,711 | ~6,470 |

01~03 이후 실측(2026-09-06):

| 파일 | 착수 전 | 현재 |
|---|---:|---:|
| `facade/parser_processor.py` | 2,497 | **1,605** |
| `facade/chunking_processor.py` | 1,536 | **1,217** |
| `facade/intelligent_processor.py` | 1,458 | **1,098** |
| `facade/convert_processor.py` | 1,600 | **1,288** |
| 합계(attachment 제외) | 7,091 | **5,208** |
| 신규 공용 모듈 | — | +958 (`common/docling_runtime.py` 523 · `common/parser_config.py` 95 · `serialize/parse_format.py` 340) |

parser 는 목표치(~1,400)에 못 미쳤다. 테스트가 요구하는 얇은 래퍼와 남긴
`GenericDocumentLoader`(위 결정 3) 때문이며, **대용량 파일 가드 임계 1,200줄을 여전히 넘는다**
— 04 의 표면 정리가 그만큼 중요하다.

parser 목표치는 검증에서 **~1,000 이 350~450줄 낙관**으로 판정돼 상향했다. 산술:
2,497 − 387(02) − 765(3a+3b+3c) = 1,345 이고, 테스트가 요구하는 얇은 래퍼를 더하면
1,400~1,450 이 현실적이다. **이 저장소 자신의 대용량 파일 가드 임계가 1,200줄이므로
리팩터링 후에도 그 선을 넘는다** — 04 의 표면 정리가 그만큼 중요해진다.

중복 실측:

- 메서드 단위 유사도 0.85 이상인 사실상 동일 코드 **502줄**
  (`_create_converters` 1.00, `enrich_metadata` 1.00, `check_glyphs` 1.00, `ocr_all_table_cells` 0.92)
- `__init__` 중복 **약 680줄** — intelligent↔convert 유사도 0.97(동일 303줄),
  chunking 205줄, parser 임베드본 174줄

## 범위 밖

- **신규 기능 전부** — 새 설정 키, 확장 메커니즘, 플러그인 지점
- `facade/legacy/` — 별도 배포 단위
- `docling/` — 손대면 wheel 재빌드가 강제된다
- `attachment_processor.py` — docling 런타임이 없어 02 의 대상이 아니다
- 동작 변경 · 성능 개선 · 조사 중 발견되는 곁가지 결함 — 별도 이슈로 분리
- custom_fields 스키마 전환 — **이미 끝났다.** `resource/custom_field_*.yaml` 17/17 이
  `schema: v2` 다(커밋 `2a28195d`, 2026-09-04). 루트 `CLAUDE.md:166` 의 "전환하지 말 것"
  서술은 낡았다. v1 표기도 계속 동작하므로(`config_v2.is_v2` 로만 갈린다) 전환 자체는 범위 밖

## 검증 원칙

- A/B 대조와 골든 대조가 1차 관문이다. 유닛 테스트는 그 다음이다.
- 유닛은 **바꾼 모듈을 쓰는 파일만** 지정해서 돌린다. 전체는 201초 + 사전 실패 21건이라
  단계마다 돌리면 비용이 크다.
- 전체 유닛은 각 단계 **종료 시 1회**와 PR 직전에 돌린다.
- 항상 `--color=no` 를 유지한다. `addopts` 의 `-p no:cacheprovider` 때문에 `--lf` 는 쓸 수 없다.


## 검증 이력

계획은 서브에이전트 독립 검증을 거쳤다. 검증에서 나온 정정은 각 문서에 반영돼 있다.

| 대상 | 상태 | 주요 결과 |
|---|---|---|
| 03 (parser 분해) | 완료 | 9건 정정. 3c 는 순수 함수가 아니고 위험이 가장 높다 |
| 05 (기능 경계) | 완료 | v2 가 이미 출고 표기. `transform` 은 2종이 아니라 9종. 경계는 4가 아니라 8 |
| 고객 개발자 관점 | 완료 | 07 신설의 근거. 고객이 읽는 매뉴얼과 골든 커버리지 구멍 |
| 01·02 기술 타당성 | 완료 | 01 의 미호출 판정 15건은 전부 참. 다만 삭제 범위가 필수 대입 2개를 삼켰고, 02 는 속성 집합이 갈라져 있어 단순 이동이 아니다 |
| 검증 설계 타당성 | 완료 | **골든이 3c 의 절반을 실행하지 않는다.** LLM soft-fail 이 "빈 값으로 일치" 를 만든다 |

**5건 전부 완료됐고 모든 문서에 정정이 반영됐다.**

착수 순서에 영향을 주는 결론 세 가지:

1. **07 이 가장 먼저다** — 고객 수정분이 릴리스에서 어떻게 되는지가 정해지지 않으면
   04~06 의 목적이 성립하지 않는다.
2. **00 이 그다음이고, `--output-format` 추가가 그 안에 포함돼야 한다** —
   그것 없이는 03 의 3c 가 안전망 없이 진행된다.
3. **01 은 착수 가능하다** — 미호출 판정이 독립 검증을 통과했다. 삭제 범위만 개정판대로 쓴다.
